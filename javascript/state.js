/*import { imagesData } from "../data/imagesData.js";
import { responses } from '../data/responses.js';


export let activeImages = [];
export let inactiveImages = imagesData.map(img => img.id);


export let activeQuotes = [];
export let inactiveQuotes = responses.map(quote => quote.id);


console.log("Inactive Image IDs:", inactiveImages);
// Add this function to reset state
export function resetState() {
  activeImages = [];
  inactiveImages = imagesData.map(img => img.id);
  activeQuotes = [];
  inactiveQuotes = responses.map(quote => quote.id);
} */